# 🚀 虚拟仿真实训教学管理平台 - 快速启动指南

## 📋 前置准备

确保已安装以下软件:

### 必需软件
- ✅ **JDK 17+** - Java开发环境
- ✅ **Maven 3.6+** - 项目构建工具
- ✅ **Node.js 18+** - 前端运行环境
- ✅ **MySQL 8.0+** - 数据库
- ✅ **Redis 6.0+** - 缓存服务器

### 可选软件
- IntelliJ IDEA / Eclipse - Java IDE
- VSCode - 前端开发工具
- Navicat / DBeaver - 数据库管理工具
- RedisDesktopManager - Redis管理工具

## 🔧 环境配置

### 1. 启动MySQL

**Windows:**
```bash
# 启动MySQL服务
net start MySQL

# 或在服务管理器中启动
```

**Linux/Mac:**
```bash
sudo service mysql start
# 或
sudo systemctl start mysql
```

### 2. 启动Redis

**Windows:**
```bash
# 进入Redis目录
cd C:\Program Files\Redis
redis-server.exe redis.windows.conf
```

**Linux/Mac:**
```bash
redis-server
# 或后台启动
redis-server --daemonize yes
```

### 3. 初始化数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库并初始化
mysql> source E:\homework\2025last\work\test\database\schema.sql

# 或者使用以下命令
mysql -u root -p < E:\homework\2025last\work\test\database\schema.sql
```

**验证数据库:**
```sql
USE virtual_training_platform;
SHOW TABLES;
SELECT * FROM t_user;
```

应该能看到30+张表和默认管理员账号。

## 🎯 启动项目

### 方式一: 命令行启动

#### 1. 启动后端

```bash
# 进入后端目录
cd E:\homework\2025last\work\test\backend

# 方式1: 使用Maven启动(推荐开发环境)
mvn spring-boot:run

# 方式2: 打包后启动(推荐生产环境)
mvn clean package
java -jar target/virtual-training-platform.jar
```

**启动成功标志:**
```
========================================
虚拟仿真实训教学管理平台启动成功!
接口文档地址: http://localhost:8080/api/doc.html
========================================
```

#### 2. 启动前端

```bash
# 进入前端目录
cd E:\homework\2025last\work\test\frontend

# 安装依赖(首次启动)
npm install

# 启动开发服务器
npm run dev
```

**启动成功标志:**
```
VITE v5.0.11  ready in 1234 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.1.100:5173/
```

### 方式二: IDE启动

#### IntelliJ IDEA启动后端

1. 打开项目: `File → Open → 选择backend文件夹`
2. 等待Maven依赖下载完成
3. 找到 `VirtualTrainingApplication.java`
4. 右键 → `Run 'VirtualTrainingApplication'`

#### VSCode启动前端

1. 打开项目: `File → Open Folder → 选择frontend文件夹`
2. 打开终端: `` Ctrl + ` ``
3. 运行: `npm install` (首次)
4. 运行: `npm run dev`

## 🌐 访问地址

### 后端服务
- **API地址**: http://localhost:8080/api
- **接口文档**: http://localhost:8080/api/doc.html
- **Druid监控**: http://localhost:8080/api/druid (用户名/密码: admin/admin)

### 前端应用
- **访问地址**: http://localhost:5173
- **默认账号**: admin
- **默认密码**: admin123

## ✅ 功能验证

### 1. 测试登录

1. 访问 http://localhost:5173
2. 输入用户名: `admin`
3. 输入密码: `admin123`
4. 点击登录

**成功标志:**
- 跳转到首页
- 显示用户信息
- 左侧菜单正常显示

### 2. 测试Token存储

**检查Redis:**
```bash
# 连接Redis
redis-cli

# 查看登录Token
127.0.0.1:6379> keys login:user:*
127.0.0.1:6379> get login:user:{你的token}

# 查看过期时间
127.0.0.1:6379> ttl login:user:{你的token}
```

应该能看到存储的LoginUser对象和过期时间(604800秒=7天)

### 3. 测试API接口

**使用Swagger文档:**
1. 访问 http://localhost:8080/api/doc.html
2. 点击"认证管理"
3. 测试登录接口: `POST /v1/auth/login`
4. 测试获取用户信息: `GET /v1/auth/user-info`

## 🐛 常见问题

### 1. 后端启动失败

**问题: 数据库连接失败**
```
Error: Could not create connection to database server
```

**解决方案:**
- 检查MySQL是否启动: `netstat -ano | findstr 3306`
- 检查用户名密码: `backend/src/main/resources/application-dev.yml`
- 检查数据库是否存在: `SHOW DATABASES LIKE 'virtual_training_platform';`

**问题: Redis连接失败**
```
Error: Unable to connect to Redis
```

**解决方案:**
- 检查Redis是否启动: `redis-cli ping` (应返回PONG)
- 检查端口是否被占用: `netstat -ano | findstr 6379`
- 检查Redis配置: `application-dev.yml`

### 2. 前端启动失败

**问题: 依赖安装失败**
```
npm ERR! code ECONNRESET
```

**解决方案:**
```bash
# 切换npm镜像源
npm config set registry https://registry.npmmirror.com

# 清除缓存重新安装
npm cache clean --force
npm install
```

**问题: 端口被占用**
```
Port 5173 is already in use
```

**解决方案:**
```bash
# Windows
netstat -ano | findstr 5173
taskkill /F /PID {进程ID}

# Linux/Mac
lsof -i:5173
kill -9 {进程ID}

# 或修改vite.config.js中的port配置
```

### 3. 登录失败

**问题: 用户名或密码错误**
- 确认使用正确的账号: admin / admin123
- 检查数据库中用户是否存在: `SELECT * FROM t_user WHERE username='admin';`

**问题: Token验证失败**
- 检查JWT密钥配置: `application.yml` 中的 `training.jwt.secret`
- 检查Token格式: 应为 `Bearer {token}`

### 4. 跨域问题

**问题: CORS错误**
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**解决方案:**
- 检查 `CorsConfig.java` 中的allowed-origins配置
- 确认前端请求地址正确: http://localhost:5173
- 重启后端服务

## 📊 项目结构说明

```
test/
├── database/           # 数据库脚本
│   └── schema.sql     # 建表SQL(30+张表)
├── backend/            # 后端项目(Spring Boot)
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/training/
│   │   │   │   ├── common/      # 公共模块
│   │   │   │   └── module/      # 业务模块
│   │   │   └── resources/
│   │   │       ├── application.yml
│   │   │       └── mapper/
│   │   └── test/
│   └── pom.xml
├── frontend/           # 前端项目(Vue 3)
│   ├── src/
│   │   ├── api/        # API接口
│   │   ├── components/ # 组件
│   │   ├── router/     # 路由
│   │   ├── store/      # 状态管理
│   │   ├── utils/      # 工具函数
│   │   └── views/      # 页面
│   ├── package.json
│   └── vite.config.js
└── README.md
```

## 🎯 下一步

### 已完成功能 ✅
- [x] 用户认证(登录/登出)
- [x] JWT Token + Redis存储
- [x] 权限控制
- [x] 响应式布局
- [x] 首页展示

### 待开发功能 📝
- [ ] 新闻公告模块
- [ ] 用户中心模块
- [ ] 资源中心模块
- [ ] 实训中心模块
- [ ] 实验室模块
- [ ] 共享开放模块

## 📞 技术支持

如遇到问题:
1. 查看控制台错误日志
2. 检查数据库和Redis连接
3. 查看 `用户认证模块实现说明.md`
4. 参考 `项目设计文档.md`

---

**祝你开发顺利!** 🎉
